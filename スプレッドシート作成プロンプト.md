# スプレッドシートの用意（Gemini に投げるプロンプト付き）

九九チャレンジの記録を Google スプレッドシートに集めるための手順です。

---

## 全体の流れ

1. Google スプレッドシートを新規作成する
2. **下の「Gemini に貼るプロンプト」** で、シートの枠と名簿を作らせる
3. `コード.gs` の中身を Apps Script に貼り、ウェブアプリとしてデプロイする
4. デプロイで出た URL を `js/config.js` の `GAS_URL` に貼る
5. GitHub に push する

所要 15〜20分ほどです。手順3以降は、このフォルダの `コード.gs` 冒頭のコメントにも同じ説明があります。

---

## Gemini に貼るプロンプト

新規スプレッドシートを開いた状態で、Gemini（スプレッドシート内の Gemini、または Gemini アプリ）に以下をそのまま貼ってください。

> **注意：** Gemini が作った結果は必ず目で確認してください。とくに **「ID」列が「書式なしテキスト」になっているか**は要チェックです。ここが数値書式のままだと、後で ID の照合に失敗して記録が誰のものか分からなくなります。

```text
Google スプレッドシートに、小学校の九九練習アプリの記録用シートを3枚つくってください。
シート名・列名・並び順は、以下のとおり一字一句そのままにしてください（アプリ側がこの名前で読み書きします）。

■ シート1：名簿
1行目を見出しにして、次の4列をこの順に作ってください。
  A列: ID
  B列: なまえ
  C列: クラス
  D列: つかう

- A列「ID」は必ず「書式なしテキスト」に設定してください。数値書式だと先頭の0が消えたり、照合に失敗します。
- D列「つかう」はチェックボックスにして、初期値はすべてチェックあり（TRUE）にしてください。
  チェックを外した子は、アプリの名前一覧に出なくなります。
- 見出し行を固定表示にしてください。
- サンプルとして、次の3行を入れてください。
  201, さとう たろう, 2年1組, TRUE
  202, すずき はなこ, 2年1組, TRUE
  203, たなか じろう, 2年1組, TRUE
- ID は出席番号でも学籍番号でもかまいませんが、重複しないようにしてください。

■ シート2：きろく
1問ごとの記録が1行ずつ追記されます。1行目を見出しにして、次の13列をこの順に作ってください。
  日時 / ID / なまえ / モード / レベル / a / b / こたえ / にゅうりょく / せいご / じかんぎれ / そうきじかん / ごうけいじかん

- 「日時」列は yyyy/mm/dd hh:mm:ss の表示形式にしてください。
- 「ID」列は「書式なしテキスト」にしてください。
- 「せいご」「じかんぎれ」は 1 か 0 が入ります（1 が該当）。
- 「そうきじかん」「ごうけいじかん」の単位はミリ秒です。見出しの下にメモは入れず、列名は上記のままにしてください。
- 見出し行を固定表示にしてください。データ行は空のままで構いません。

■ シート3：セッション
1回の練習ごとに1行ずつ追記されます。1行目を見出しにして、次の11列をこの順に作ってください。
  日時 / ID / なまえ / モード / レベル / だん / もんすう / せいかいすう / かかった時間 / さいだいれんぞく / とうたつすう

- 「日時」列は yyyy/mm/dd hh:mm:ss の表示形式にしてください。
- 「ID」列は「書式なしテキスト」にしてください。
- 「かかった時間」の単位はミリ秒です。
- 「さいだいれんぞく」はレベル99のとき、「とうたつすう」は九九マスターへの挑戦のときだけ値が入り、それ以外は空欄になります。
- 見出し行を固定表示にしてください。データ行は空のままで構いません。

3枚とも、見出し行は太字・背景色を薄いグレーにしてください。
既定で作られる「シート1」など、上記以外のシートがあれば削除してください。
```

### 名簿を一気に入れたいとき（追加プロンプト）

```text
「名簿」シートに、次の名簿を3行目以降に追加してください。
ID は出席番号を使い、クラスはすべて「2年1組」、「つかう」はすべてチェックありにしてください。
ID 列は「書式なしテキスト」のままにしてください。

（ここに 出席番号と名前の一覧を貼る）
```

### 学級ごとの様子を見たいとき（追加プロンプト）

記録がたまってから使ってください。

```text
このスプレッドシートに「まとめ」シートを追加して、次の表を数式で作ってください。
元データは「きろく」シートです。行は名簿の子ども（ID となまえ）、列は次の項目にしてください。

- といた もんだい数 … その子の行数
- 正答率 … せいご列の平均（パーセント表示、小数第1位まで）
- じかんぎれ率 … じかんぎれ列の平均（パーセント表示、小数第1位まで）
- そうきじかんの中央値 … そうきじかん列の中央値（ミリ秒、空欄は除く）
- さいごに といた日 … 日時列の最大値

数式は QUERY か FILTER と AVERAGEIFS / MEDIAN などを使い、
名簿が増えても自動で反映されるように、範囲は列全体で指定してください。
```

---

## アプリ側の設定

デプロイで出た `https://script.google.com/macros/s/××××/exec` を貼ります。

`js/config.js`

```js
export const GAS_URL = 'ここに exec で終わる URL を貼る';
```

**空のままにしておくと「ローカルモード」**（この iPad の中だけに記録が残る）で動きます。まず動きを見たいときや、ネットのない場所で試すときは空のままでかまいません。

---

## シートモードにすると何が変わるか

| | ローカルモード（GAS_URL 空） | シートモード（GAS_URL あり） |
|---|---|---|
| 名前 | この iPad に保存される | **保存されない**（毎回、名簿から選ぶ） |
| 名簿の追加・削除 | アプリの せんせいメニュー | スプレッドシートの「名簿」シート |
| 記録の保存先 | この iPad | スプレッドシート |
| 九九表の履歴 | この iPad の記録から | 名前を選んだときにシートから読む |
| CSV 書き出し | せんせいメニューから | スプレッドシートから直接 |
| 通信できないとき | ― | 端末に退避し、次につながったとき自動で再送 |

シートモードでは、iPad に残るのは **送信できなかった記録（ID のみ・名前なし）** と **制限時間などの設定** だけです。

---

## 通信の回数

1人が1回練習するあいだの通信は 3回だけです。

| いつ | 何を | 向き |
|---|---|---|
| アプリを開いたとき | 名簿を読む | GET |
| 名前を選んだとき | その子の過去記録を読む | GET |
| 1回 終わったとき | その回の記録を書く | POST |

問題を1問解くごとの通信はありません。授業中に何十台から一斉に使っても、通信は開始時と終了時に集中するだけです。

---

## うまくいかないときの確認

| 症状 | 見るところ |
|---|---|
| 名前一覧が空・エラーが出る | デプロイの「アクセスできるユーザー」が**全員**か。URL が `/exec` で終わっているか |
| 記録がシートに増えない | せんせいメニューの「スプレッドシート連携」に未送信件数が出ていないか |
| 記録が誰のものか合わない | 「名簿」シートの ID 列が**書式なしテキスト**か |
| コードを直したのに反映されない | デプロイを**新しいバージョン**で作り直したか |
| アプリの画面が古いまま | `sw.js` の `CACHE_VERSION` を上げて push したか |
