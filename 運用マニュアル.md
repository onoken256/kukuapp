# 九九チャレンジ 運用マニュアル

公開までの手順と、後日変更するときの手順をまとめたものです。

---

# 第1部　公開までの手順

所要 30〜40分。**A → B → C** の順に進めてください。
「まず動かしてみたい」だけなら **A だけ**で完結します（記録はiPadの中だけに残ります）。

---

## A. ローカルで動かして確かめる（5分）

ターミナルでこのフォルダに移動して、静的サーバを立てます。

```bash
cd "/Users/tcsstaff/Documents/Claude/九九アプリ" && python3 -m http.server 8765
```

ブラウザで `http://localhost:8765` を開きます。止めるときはターミナルで `Ctrl + C`。

> **`file://` で index.html を直接開いても動きません。** ES Modules と Service Worker が
> ブラウザにブロックされるためです。必ず上のサーバ経由で開いてください。

### 確かめること

- [ ] 名前を追加して、ホームに3つのモードが並ぶ
- [ ] 九九チャレンジ：レベル2で、答えずに待つと時間切れになり、正答の再入力を求められる
- [ ] 九九マス計算：81マス埋めると青／赤の判定が出る
- [ ] 九九ならべ：カードが順に入り、「けってい」で判定が出る
- [ ] 九九表：解いた問題に色がつく
- [ ] 歯車を1.5秒長押しで せんせいメニューが開く
- [ ] ウィンドウを縦長にすると「がめんを よこむきに してね」が出る

---

## B. GitHub Pages で公開する（15分）

### B-1. Git リポジトリを作る

このフォルダはまだ Git 管理されていません。最初の1回だけ実行します。

```bash
cd "/Users/tcsstaff/Documents/Claude/九九アプリ" && git init -b main && git add -A && git commit -m "九九チャレンジ 初版"
```

### B-2. GitHub にリポジトリを作って push

GitHub で新しいリポジトリを作ります（**README や .gitignore は追加しない**を選択）。
リポジトリ名は例として `kuku` とします。

```bash
git remote add origin https://github.com/【あなたのユーザー名】/kuku.git && git push -u origin main
```

### B-3. GitHub Pages を有効にする

リポジトリのページで **Settings ▸ Pages** を開き、

| 項目 | 値 |
|---|---|
| Source | Deploy from a branch |
| Branch | `main` / `(root)` |

**Save** を押します。1〜2分で
`https://【ユーザー名】.github.io/kuku/`
が使えるようになります。

> リポジトリは **Public** にしてください。Private のままだと GitHub Pages は
> 有料プランでないと使えません。公開されるのはアプリのコードだけで、
> 子どもの記録は含まれません。

### B-4. iPad のホーム画面に追加する

1. Safari で上のURLを開く
2. 共有ボタン（□に↑）▸ **ホーム画面に追加**
3. 追加されたアイコンから起動する

これで全画面表示になり、オフラインでも動きます。

---

## C. スプレッドシートに記録を集める（20分）

**この手順をやると、子どもの名前が iPad に一切保存されなくなります。**
学校で運用する場合はここまで進めてください。

詳しい手順とGeminiに貼るプロンプトは **[スプレッドシート作成プロンプト.md](スプレッドシート作成プロンプト.md)** にあります。流れだけ再掲します。

1. Google スプレッドシートを新規作成
2. `スプレッドシート作成プロンプト.md` のプロンプトを Gemini に貼り、`名簿`・`きろく`・`セッション` の3シートを作らせる
   - **「ID」列が「書式なしテキスト」になっているか必ず目で確認**（ここが数値書式だと記録が誰のものか合わなくなります）
3. `名簿` シートに子どもを入れる
4. 拡張機能 ▸ Apps Script に **[コード.gs](コード.gs)** の中身を貼る
5. デプロイ ▸ 新しいデプロイ ▸ **ウェブアプリ**
   - 実行するユーザー：**自分**
   - アクセスできるユーザー：**全員**
6. 出てきた `.../exec` で終わるURLを `js/config.js` に貼る

```js
export const GAS_URL = 'https://script.google.com/macros/s/××××/exec';
```

7. **`sw.js` の `CACHE_VERSION` を1つ上げる**（`kuku-v2` → `kuku-v3`）
8. push する

```bash
git add -A && git commit -m "スプレッドシート連携を有効化" && git push
```

### 動作確認

- [ ] アプリを開くと、名簿の名前が一覧に出る（追加欄は出ない）
- [ ] 1回練習を終えると、`きろく`シートに行が増える
- [ ] せんせいメニューの「スプレッドシート連携」に「未送信の記録はありません」と出る

---

# 第2部　後日の変更手順

## 変更したら必ずやること（3ステップ）

どんな変更でも、この3つはセットです。

### ① `sw.js` の `CACHE_VERSION` を1つ上げる

```js
const CACHE_VERSION = 'kuku-v2';   // ← v3, v4 … と上げていく
```

**これを忘れると、子どものiPadに古い画面が残り続けます。**
アプリは「まずキャッシュを見せる」作りなので、バージョン文字列が変わらない限り
新しいファイルを取りに行きません。

### ② push する

```bash
cd "/Users/tcsstaff/Documents/Claude/九九アプリ" && git add -A && git commit -m "変更内容をここに書く" && git push
```

### ③ iPad で開き直す

ホーム画面のアイコンから起動し、**一度閉じてもう一度開く**と新しい版になります。
GitHub Pages の反映に数分かかることがあるので、すぐ変わらなければ10分ほど置いて再度開いてください。

---

## どこを直すと何が変わるか

### いちばんよく触る：`js/constants.js`

| 変えたいこと | 直す場所 |
|---|---|
| 「できた（青）」と認める速さ | `FAST_LATENCY_MS = 1500`（ミリ秒） |
| 判定に使う回数 | `MASTERY_WINDOW = 3`（直近3回） |
| せんせいメニューで選べる制限時間 | `TIME_LIMIT_OPTIONS = [3, 4, 5, 7]` |
| 制限時間の初期値 | `DEFAULT_TIME_LIMIT_SEC = 5` |
| 1人あたりの記録保持件数 | `ATTEMPT_LIMIT = 3000` |
| 正解してから次の問題までの間 | `CORRECT_PAUSE_MS = 800` |
| せんせいメニューの長押し時間 | `TEACHER_HOLD_MS = 1500` |
| 九九チャレンジのレベル構成 | `LEVELS` の配列 |
| 九九マス計算のレベル構成 | `GRID_LEVELS` の配列 |

しきい値は**このファイル1か所**にまとまっています。結果画面の「あとちょっと」も
九九表の色分けも同じ `FAST_LATENCY_MS` を見ているので、片方だけずれることはありません。

### レベルを1つ足したいとき（例：九九チャレンジ）

`js/constants.js` の `LEVELS` に1件足すだけです。画面のボタンは自動で増えます。

```js
{
  id: 6, label: 'レベル6', title: 'せつめい（短く）',
  desc: 'カードに出る せつめい文',
  dansRequired: 3,      // 段選択で選ばせる数（0 なら段選択なし）
  order: 'random',      // 'seq' = 1〜9の順 / 'random' = ばらばら
  timed: true,          // 制限時間を使うか
  count: 27,            // 出題数
  stopOnFail: false,    // 1回失敗したら即終了するか
  streak: false,        // 連続正解数を表示・記録するか
},
```

### 見た目を変えたいとき：`css/style.css`

ファイル先頭の `:root` に色がまとまっています。

```css
--c-primary: #1d4ed8;   /* ボタンなどの基本色 */
--s-good:    #2f7fd6;   /* できた（青） */
--s-almost:  #f0b323;   /* あとちょっと（黄） */
--s-weak:    #e0524a;   /* にがて（赤） */
--s-untried: #d3d0c8;   /* みちょうせん（グレー） */
```

### 文言を変えたいとき：`index.html`

画面の固定文言はほぼ `index.html` に直接書いてあります。
ボタンの中身も日本語のまま置いてあるので、そのまま書き換えれば反映されます。

### スプレッドシート側を変えたいとき：`コード.gs`

`コード.gs` を直したら、Apps Script で
**デプロイ ▸ デプロイを管理 ▸ 鉛筆アイコン ▸ バージョン「新バージョン」▸ デプロイ**
としてください。**URLは変わりません**ので、`js/config.js` はそのままで大丈夫です。

> 「新しいデプロイ」を選ぶと**別のURLが発行されてしまいます**。
> 直したのに反映されないときは、たいていここです。

---

## ★ 新しい .js ファイルを足したときの注意

ファイルを新規作成したら、**`sw.js` の `PRECACHE` 一覧に1行足してください。**

```js
const PRECACHE = [
  './',
  './index.html',
  ...
  './js/あたらしいファイル.js',   // ← ここに足す
];
```

足し忘れると、オフラインのときだけ真っ白になります。オンラインでは動いてしまうので
気づきにくい種類の不具合です。

---

## ファイルの役割

| ファイル | 役割 |
|---|---|
| `index.html` | 全画面のHTML。文言はここ |
| `css/style.css` | 見た目すべて |
| `js/constants.js` | **しきい値・レベル定義。調整はまずここ** |
| `js/config.js` | GAS_URL（空ならローカルモード） |
| `js/main.js` | 画面の切りかえ・ホーム・レベル選択・横向き判定 |
| `js/quiz.js` | モード①九九チャレンジ＋その結果画面 |
| `js/gridmode.js` | モード②九九マス計算 |
| `js/sortmode.js` | モード③九九ならべ |
| `js/heatmap.js` | 九九表の描画 |
| `js/mastery.js` | **色分けの判定ロジック**（描画から分離） |
| `js/storage.js` | 保存層の入口。ローカル／シートを切り替える |
| `js/storage-local.js` | この端末に保存する版 |
| `js/storage-remote.js` | スプレッドシートに保存する版 |
| `js/teacher.js` | せんせいメニュー・CSV出力 |
| `js/timer.js` | 制限時間とストップウォッチ |
| `js/keypad.js` | 数字キーパッド（モード①②で共用） |
| `js/dialog.js` | 確認ダイアログ |
| `js/sounds.js` | 効果音（Web Audio で生成） |
| `js/questions.js` | 出題順の生成 |
| `js/profiles.js` | 名前えらび画面 |
| `sw.js` | オフライン対応。**更新時は CACHE_VERSION を上げる** |
| `manifest.json` | ホーム画面追加の設定 |
| `コード.gs` | スプレッドシート側のプログラム |
| `.claude/launch.json` | ローカル確認用の設定。消してもアプリには影響しません |

---

## 困ったときは

| 症状 | 見るところ |
|---|---|
| iPadで直したはずの画面が古いまま | `sw.js` の `CACHE_VERSION` を上げたか。上げたなら10分待って開き直す |
| オフラインだけ真っ白 | 新しく足した .js を `sw.js` の `PRECACHE` に書いたか |
| 名前一覧が空・エラーが出る | GASのデプロイが「アクセスできるユーザー：全員」か。URLが `/exec` で終わっているか |
| 記録がシートに増えない | せんせいメニューの「スプレッドシート連携」に未送信件数が出ていないか |
| 記録が誰のものか合わない | `名簿` シートの ID 列が**書式なしテキスト**か |
| GASを直したのに反映されない | 「デプロイを管理」から**新バージョン**で更新したか（新しいデプロイではない） |
| ローカルで真っ白 | `file://` で開いていないか。`python3 -m http.server` 経由で開く |
